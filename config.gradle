ext {
    minSdkVersion = 19
    targetSdkVersion = 27
    compileSdkVersion = 27
    buildToolsVersion = '27.0.2'

    supportLibraryVersion = buildToolsVersion

    test = [
            baseUrl: "abc",
            hello:"ddd"]

    qa = [baseUrl: "abc"]
}